"use client";

import React from "react";

export default function Error({ error, reset }) {
  return (
    <div>
      <h1>Error is coming in about route</h1>
      <p>
        Lorem50 Error is coming in about route Error is coming in about
        routeError is coming in about routeError is coming in about routeError
        is coming in about routeError is coming in about route
      </p>
    </div>
  );
}
